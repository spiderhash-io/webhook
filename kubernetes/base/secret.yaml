apiVersion: v1
kind: Secret
metadata:
  name: webhook-secrets
  namespace: webhook-system
  labels:
    app.kubernetes.io/name: core-webhook-module
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before deploying!
  STATS_AUTH_TOKEN: "CHANGE_ME_stats_token_here"
  CONFIG_RELOAD_ADMIN_TOKEN: "CHANGE_ME_admin_token_here"
